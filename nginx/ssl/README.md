# This is a self-signed certificate for development purposes
# In production, replace with certificates from a trusted CA

# Generate with:
# openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes

# For now, creating placeholder files (they should be replaced with actual certificates)
PLACEHOLDER_CERTIFICATE=True